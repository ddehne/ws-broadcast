{
  "name": "broadcast-server",
  "version": "1.0.0",
  "description": "An example websocket broadcast server",
  "main": "broadcast-server.js",
  "scripts": {
    "start": "node ./broadcast-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ddehne",
  "license": "ISC",
  "devDependencies": {
    "aws": "0.0.3-2",
    "aws-sdk": "^2.167.0",
    "express": "^4.16.2",
    "ws": "^3.3.2"
  }
}
